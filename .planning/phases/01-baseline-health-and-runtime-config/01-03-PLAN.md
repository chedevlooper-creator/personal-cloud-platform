---
phase: 01-baseline-health-and-runtime-config
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - package.json
  - scripts/baseline-smoke.mjs
  - README.md
  - infra/docker/.env.example
autonomous: true
requirements:
  - BASE-02
  - BASE-03
user_setup:
  - Docker Desktop or compatible Docker engine must be available for infra smoke checks.
  - `infra/docker/.env` must be created from `infra/docker/.env.example` before Compose checks.
must_haves:
  truths:
    - The repo exposes a clear baseline smoke command or documented smoke sequence.
    - DB migration readiness can be verified from a clean infra stack.
    - Stale API gateway references are removed from env examples and docs.
  artifacts:
    - scripts/baseline-smoke.mjs
    - README.md
    - infra/docker/.env.example
  key_links:
    - Smoke script must use root package scripts and DB package migration commands.
    - README must explain Docker-dependent checks separately from pure local checks.
---

<objective>
Add baseline smoke verification for infra, database migrations, and package health.

Purpose: Later phases need a repeatable way to prove the local platform can start and migrate.
Output: Smoke script or documented smoke sequence plus corrected env example.
</objective>

<execution_context>
@/Users/isahamid/Documents/untitled folder/task-chat/untitled folder/untitled folder/personal-cloud-platform/.codex/get-shit-done/workflows/execute-plan.md
@/Users/isahamid/Documents/untitled folder/task-chat/untitled folder/untitled folder/personal-cloud-platform/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/STACK.md
@.planning/codebase/TESTING.md
@.planning/phases/01-baseline-health-and-runtime-config/01-CONTEXT.md
@package.json
@infra/docker/docker-compose.yml
@infra/docker/.env.example
@packages/db/drizzle.config.ts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pure local baseline smoke command</name>
  <files>package.json, scripts/baseline-smoke.mjs</files>
  <read_first>package.json, .planning/codebase/TESTING.md</read_first>
  <action>Create `scripts/baseline-smoke.mjs` that runs pure local checks in order: package manager/version sanity, `pnpm -r --if-present typecheck`, `pnpm -r --if-present lint`, and `pnpm -r --if-present test`. Add a root script named `smoke:local` that runs it. The script must fail on the first failing command and print the exact command that failed. It must not start Docker.</action>
  <verify>pnpm smoke:local</verify>
  <acceptance_criteria>
    - `package.json` has `smoke:local`.
    - `scripts/baseline-smoke.mjs` uses Node standard library only.
    - The script exits non-zero on command failure.
  </acceptance_criteria>
  <done>Pure local baseline smoke is executable.</done>
</task>

<task type="auto">
  <name>Task 2: Add infra and migration smoke documentation</name>
  <files>README.md</files>
  <read_first>README.md, infra/docker/docker-compose.yml, packages/db/package.json</read_first>
  <action>Add a "Baseline Smoke" section to README with two tracks: pure local (`pnpm smoke:local`) and Docker-backed (`cp infra/docker/.env.example infra/docker/.env`, `pnpm infra:up`, `pnpm --filter @pcp/db migrate`, service health URLs). Include memory service on port 3005. Make clear Docker-backed checks require Docker and a local env file.</action>
  <verify>rg -n "Baseline Smoke|smoke:local|3005|infra:up|@pcp/db migrate" README.md</verify>
  <acceptance_criteria>
    - README separates pure local checks from Docker checks.
    - README includes the DB migration command.
    - README lists all service health ports including memory.
  </acceptance_criteria>
  <done>Baseline verification is documented for developers.</done>
</task>

<task type="auto">
  <name>Task 3: Correct infra env example stale gateway values</name>
  <files>infra/docker/.env.example, README.md</files>
  <read_first>infra/docker/.env.example, apps/web/src/lib/api.ts</read_first>
  <action>Remove or correct stale single-gateway assumptions in `infra/docker/.env.example`, especially `API_URL=http://localhost:4000`. Prefer explicit service URLs or omit `API_URL` if no code consumes it. Keep app/frontend URL values that are actually used. Update README to match.</action>
  <verify>! rg -n "API_URL=http://localhost:4000|localhost:4000|apps/api" infra/docker/.env.example README.md</verify>
  <acceptance_criteria>
    - No env example points at a non-existent port 4000 API gateway.
    - README and env example agree on service topology.
  </acceptance_criteria>
  <done>Infra docs no longer encode stale gateway assumptions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm smoke:local`
- [ ] `pnpm infra:up` if Docker is available and `infra/docker/.env` exists
- [ ] `pnpm --filter @pcp/db migrate` against local Postgres if infra is up
- [ ] `curl -fsS http://localhost:3001/health` after auth service is started manually
- [ ] `curl -fsS http://localhost:3005/health` after memory service is started manually
</verification>

<success_criteria>

- BASE-02 and BASE-03 are satisfied.
- Local and Docker-backed smoke paths are documented.
- DB migration readiness is explicitly verified.
- Stale gateway references are removed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-health-and-runtime-config/01-03-SUMMARY.md`
</output>
