---
phase: 01-baseline-health-and-runtime-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - apps/web/package.json
  - services/auth/package.json
  - services/workspace/package.json
  - services/runtime/package.json
  - services/agent/package.json
  - services/memory/package.json
  - services/publish/package.json
  - packages/db/package.json
  - packages/shared/package.json
  - .gitignore
  - README.md
autonomous: true
requirements:
  - BASE-01
user_setup: []
must_haves:
  truths:
    - Root typecheck command executes real package typecheck scripts instead of doing nothing.
    - README baseline commands match actual package names and service ports.
    - Generated artifact tracking policy is explicit.
  artifacts:
    - package.json
    - README.md
    - .gitignore
  key_links:
    - Root scripts must align with package-level scripts.
    - README command examples must use pnpm filters that exist in package.json files.
---

<objective>
Make baseline developer commands honest and document the generated artifact policy.

Purpose: Phase 1 needs a reliable command surface before config and infra checks can be trusted.
Output: Package scripts, README command corrections, and ignore policy updates.
</objective>

<execution_context>
@/Users/isahamid/Documents/untitled folder/task-chat/untitled folder/untitled folder/personal-cloud-platform/.codex/get-shit-done/workflows/execute-plan.md
@/Users/isahamid/Documents/untitled folder/task-chat/untitled folder/untitled folder/personal-cloud-platform/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/STACK.md
@.planning/codebase/TESTING.md
@.planning/phases/01-baseline-health-and-runtime-config/01-CONTEXT.md
@package.json
@pnpm-workspace.yaml
@README.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add real package typecheck scripts</name>
  <files>package.json, apps/web/package.json, services/auth/package.json, services/workspace/package.json, services/runtime/package.json, services/agent/package.json, services/memory/package.json, services/publish/package.json, packages/db/package.json, packages/shared/package.json</files>
  <read_first>package.json, tsconfig.base.json, apps/web/tsconfig.json, packages/shared/package.json</read_first>
  <action>Add a `typecheck` script to every workspace package that TypeScript can check. Use `tsc --noEmit` for services, `next typegen && tsc --noEmit` only if local Next 16 docs require it, otherwise use `tsc --noEmit` for `apps/web`, and `tsc --noEmit` for `packages/db` and `packages/shared`. Keep root `pnpm typecheck` as the recursive fan-out. Do not add a build script to `packages/shared`; typecheck is not a build artifact.</action>
  <verify>pnpm -r --if-present typecheck</verify>
  <acceptance_criteria>
    - Each package under `apps/*`, `services/*`, and `packages/*` has a `typecheck` script or a documented reason not to.
    - Root `pnpm typecheck` runs real work.
    - No package expects `packages/shared/dist`.
  </acceptance_criteria>
  <done>Typecheck fan-out is meaningful and succeeds or exposes real existing TypeScript errors for follow-up.</done>
</task>

<task type="auto">
  <name>Task 2: Clarify generated artifact policy</name>
  <files>.gitignore</files>
  <read_first>.gitignore, .planning/codebase/STRUCTURE.md</read_first>
  <action>Audit whether `dist/` and `*.tsbuildinfo` files are currently tracked. If they are not intended source artifacts, add ignore entries for service/package dist outputs and TypeScript incremental files. If files are already tracked, remove them from the git index without deleting user source files. Do not touch unrelated `.agent` deletions or Codex config files.</action>
  <verify>git status --short -- services packages apps | sed -n '1,120p'</verify>
  <acceptance_criteria>
    - `.gitignore` documents generated TypeScript artifacts.
    - Tracked generated outputs are either intentionally retained with a README note or removed from tracking.
    - No source `.ts` or `.tsx` files are deleted.
  </acceptance_criteria>
  <done>Generated artifact policy is explicit and the worktree does not accumulate new build noise.</done>
</task>

<task type="auto">
  <name>Task 3: Correct baseline README commands</name>
  <files>README.md</files>
  <read_first>README.md, package.json, apps/web/package.json, services/*/package.json, infra/docker/docker-compose.yml</read_first>
  <action>Update README command sections so they reflect the actual repo: no single `apps/api`, no `API_URL` expectation for port 4000, service ports 3001-3006, memory service included, root `pnpm typecheck` now meaningful after Task 1, and package filter names match package.json. Keep the README honest about MVP/simplified areas that are not yet production-complete.</action>
  <verify>rg -n "typecheck|@pcp/memory-service|3005" README.md && ! rg -n "apps/api|:4000|API_URL=http://localhost:4000" README.md</verify>
  <acceptance_criteria>
    - README does not claim an `apps/api` gateway exists.
    - README lists memory service startup on port 3005.
    - README does not say all legacy phases are complete as active truth.
    - README includes exact typecheck/test commands that work in this workspace.
  </acceptance_criteria>
  <done>Developer setup docs match executable config.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm -r --if-present typecheck`
- [ ] `pnpm -r --if-present lint`
- [ ] `pnpm -r --if-present test`
- [ ] `! rg -n "apps/api|:4000|API_URL=http://localhost:4000" README.md infra/docker/.env.example`
</verification>

<success_criteria>

- BASE-01 is satisfied.
- Root and package commands are honest.
- Generated artifact policy is explicit.
- README baseline setup is accurate.
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-health-and-runtime-config/01-01-SUMMARY.md`
</output>
