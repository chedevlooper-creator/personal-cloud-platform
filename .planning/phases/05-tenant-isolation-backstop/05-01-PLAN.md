---
phase: 5
plan: 05-01
title: Tenant isolation audit backstop
wave: 1
depends_on: []
requirements:
  - SEC-06
files_modified:
  - .planning/decisions/ADR-0001-tenant-isolation-backstop.md
  - packages/db/src/tenant-isolation-audit.test.ts
  - services/workspace/src/routes.tenant-isolation.test.ts
autonomous: true
---

# Plan 05-01: Tenant Isolation Audit Backstop

## Goal

Tenant isolation has a CI-enforced backstop even without PostgreSQL RLS.

## Tasks

<task id="05-01-T1" type="execute">
  <title>Record the isolation backstop decision</title>
  <read_first>
    - `.planning/ROADMAP.md`
    - `.planning/PROJECT.md`
    - `.planning/codebase/CONCERNS.md`
  </read_first>
  <action>
    Create `.planning/decisions/ADR-0001-tenant-isolation-backstop.md` stating that Phase 5 uses audit-tests-only for v0.1, with RLS deferred until request-scoped DB context exists.
  </action>
  <acceptance_criteria>
    - `.planning/decisions/ADR-0001-tenant-isolation-backstop.md` contains `audit-tests-only`.
    - `.planning/decisions/ADR-0001-tenant-isolation-backstop.md` contains `request-scoped DB context`.
  </acceptance_criteria>
</task>

<task id="05-01-T2" type="execute">
  <title>Add static tenant isolation audit tests</title>
  <read_first>
    - `packages/db/package.json`
    - `services/workspace/src/service.ts`
    - `services/runtime/src/service.ts`
    - `services/publish/src/service.ts`
    - `services/agent/src/orchestrator.ts`
    - `services/agent/src/routes/automation.ts`
    - `services/memory/src/service.ts`
  </read_first>
  <action>
    Add `packages/db/src/tenant-isolation-audit.test.ts`. The test must inventory every service source module that imports `db` from `@pcp/db/src/client`, require tenant-facing modules to show `userId` filters or parent ownership checks, and explicitly allow-list admin/identity/system-wide modules with rationale.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @pcp/db test` exits 0.
    - `packages/db/src/tenant-isolation-audit.test.ts` contains `TENANT_SCOPED_MODULES`.
    - `packages/db/src/tenant-isolation-audit.test.ts` contains `INTENTIONAL_SYSTEM_MODULES`.
  </acceptance_criteria>
</task>

<task id="05-01-T3" type="execute">
  <title>Add forged JWT red-team route test</title>
  <read_first>
    - `services/workspace/src/routes.ts`
    - `packages/db/src/auth-request.ts`
    - `services/workspace/src/service.ts`
  </read_first>
  <action>
    Add `services/workspace/src/routes.tenant-isolation.test.ts`. The test must send a JWT-shaped bearer token resolving to an attacker user, request a victim workspace id, assert `404`, and assert the workspace lookup includes the attacker `userId` rather than the victim `userId`.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @pcp/workspace-service exec vitest run src/routes.tenant-isolation.test.ts` exits 0.
    - The test contains `rejects forged JWT cross-tenant workspace access`.
    - The test asserts `response.statusCode` is `404`.
  </acceptance_criteria>
</task>

<task id="05-01-T4" type="verify">
  <title>Run phase verification</title>
  <read_first>
    - `package.json`
    - `AGENTS.md`
  </read_first>
  <action>
    Run targeted tests, then root verification commands: `pnpm --filter @pcp/db test`, `pnpm --filter @pcp/workspace-service test`, `pnpm typecheck`, and `pnpm test`.
  </action>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0.
    - `pnpm test` exits 0.
  </acceptance_criteria>
</task>

## Verification Criteria

- ADR exists and records audit-tests-only with rationale.
- Static audit fails closed for new direct service DB modules.
- Red-team forged bearer/JWT cross-tenant workspace access is rejected.
- Targeted and root test commands pass.

## Must Haves

- SEC-06 is covered by explicit audit-tests-only implementation.
- No production code path is weakened to make the tests pass.
- RLS is deferred with a concrete rationale, not silently skipped.
