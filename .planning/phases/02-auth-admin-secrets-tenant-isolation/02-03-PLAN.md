---
phase: 02-auth-admin-secrets-tenant-isolation
plan: 03
type: execute
wave: 2
depends_on:
  - '02-01'
  - '02-02'
files_modified:
  - services/agent/src/routes/automation.ts
  - services/agent/src/routes/automation.test.ts
requirements:
  - TENANT-01
  - TENANT-02
  - AUTO-01
  - TENANT-03
autonomous: true
user_setup: []
---

# Plan 02-03: Automation Tenant Scope

<objective>
Make automation CRUD/run/history operations consistently scope by authenticated user ownership.
</objective>

<tasks>

<task type="auto">
<name>Fix automation auth helper</name>
<files>services/agent/src/routes/automation.ts</files>
<action>Remove non-null assertions from session validation, reject missing/expired sessions cleanly, and return only authenticated `userId`.</action>
<verify>pnpm --filter @pcp/agent-service typecheck</verify>
</task>

<task type="auto">
<name>Add user filters to automation mutations and reads</name>
<files>services/agent/src/routes/automation.ts</files>
<action>Scope update/delete/manual run/runs queries by both automation ID and authenticated user ID. On list, apply optional workspace filter only in addition to user ownership. Ensure queue payload uses authenticated user ID after ownership check.</action>
<verify>pnpm --filter @pcp/agent-service typecheck</verify>
</task>

<task type="auto">
<name>Add automation tenant tests</name>
<files>services/agent/src/routes/automation.test.ts</files>
<action>Add tests proving cross-user update/delete/run/runs attempts return 404 or no-op without touching another user's automation.</action>
<verify>pnpm --filter @pcp/agent-service test</verify>
</task>

</tasks>

<verification>

- `pnpm --filter @pcp/agent-service typecheck`
- `pnpm --filter @pcp/agent-service test`
- `pnpm smoke:local`

</verification>
