---
phase: 02-auth-admin-secrets-tenant-isolation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/publish/src/routes.ts
  - services/publish/src/service.ts
  - services/publish/src/routes.test.ts
  - apps/web/src/app/(main)/hosting/page.tsx
requirements:
  - TENANT-01
  - TENANT-02
  - HOST-02
  - TENANT-03
autonomous: true
user_setup: []
---

# Plan 02-02: Publish Service Identity Derivation

<objective>
Remove client-supplied `userId` from publish APIs and derive ownership from the session cookie.
</objective>

<tasks>

<task type="auto">
<name>Add publish session auth helper</name>
<files>services/publish/src/routes.ts</files>
<action>Add a local helper that reads `request.cookies.sessionId`, validates the session against `sessions`, rejects expired sessions, and returns `session.userId`. Keep unauthenticated responses 401.</action>
<verify>pnpm --filter @pcp/publish-service typecheck</verify>
</task>

<task type="auto">
<name>Remove userId from publish route schemas</name>
<files>services/publish/src/routes.ts</files>
<action>Remove `userId` from create/update/start/stop/restart bodies and list/delete query strings. Supply authenticated `userId` to `PublishService` internally. Ensure update does not spread identity fields into DB `.set()`.</action>
<verify>pnpm --filter @pcp/publish-service typecheck</verify>
</task>

<task type="auto">
<name>Update web hosting calls</name>
<files>apps/web/src/app/(main)/hosting/page.tsx</files>
<action>Stop sending `userId` in publish API request bodies/query params. Keep workspace ID where it is part of the resource filter.</action>
<verify>pnpm --filter web typecheck</verify>
</task>

<task type="auto">
<name>Add publish tenant tests</name>
<files>services/publish/src/routes.test.ts</files>
<action>Add tests proving route handlers use the session user rather than a spoofed client `userId`, and return 401 for missing/expired sessions.</action>
<verify>pnpm --filter @pcp/publish-service test</verify>
</task>

</tasks>

<verification>

- `pnpm --filter @pcp/publish-service typecheck`
- `pnpm --filter @pcp/publish-service test`
- `pnpm --filter web typecheck`

</verification>
